name: 'my_new_project'
version: '1.0.0'
config-version: 2

profile: 'default'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  my_new_project:
    facebooks_ads:
      +schema: marketing_dbt
      +materialized: table

      staging:
        +materialized: table

      intermediate:
        +materialized: table

      mart:
        +materialized: table

    google_ads:
      +schema: marketing_dbt
      +materialized: table

      staging:
        +materialized: table

      intermediate:
        +materialized: table

      mart:
        +materialized: table

    linkedin_ads:
      +schema: marketing_dbt
      +materialized: table

      staging:
        +materialized: table

      intermediate:
        +materialized: table

      mart:
        +materialized: table
